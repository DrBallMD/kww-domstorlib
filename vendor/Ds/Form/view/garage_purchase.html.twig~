{% extends 'base_form.html.twig' %}

{% block regular_fields %}
    {{ form.getForm('car_count').renderLabel() }} {{ form.getForm('car_count').render() }}
    {% include 'blocks/district_city.html.twig' %}
    {{ form.getForm('price').render() }}
    {{ form.getForm('type').renderLabel('“ип:') }} {{ form.getForm('type').render() }}
{% endblock %}

{% block option_fields %}
    {{ form.getForm('long').render({unit: 'м.'}) }}
    {{ form.getForm('width').render({unit: 'м.'}) }}
{% endblock %}