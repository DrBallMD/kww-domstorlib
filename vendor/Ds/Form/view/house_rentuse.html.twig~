{% extends 'base_form.html.twig' %}

{% block regular_fields %}
<div class="row-fluid form-row">
    <div class="span5 rooms">{{ form.getForm('rooms').renderLabel(null, {class: 'label-heads'}) }} {{ form.getForm('rooms').render() }}</div>
    <div class="span4 district">
      {% include 'blocks/district_city.html.twig' %}
      <div class="span3 region">{{ form.getRegionLinks() }}</div>
    </div>
</div>
<div class="row-fluid form-row">
    <div class="span5 price rent">{{ form.getForm('rent').render({ label: 'Ѕюджет:' }) }}</div>
    <div class="span5 type_house">{{ form.getForm('type').renderLabel('“ип дома:', {class: 'label-heads'}) }} {{ form.getForm('type').render() }}</div>
</div>
{% endblock %}

{% block optional %}{% endblock %}
{% block optional_trigger %}{% endblock %}